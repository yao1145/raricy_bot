# Raricy 站内聊天机器人设计文档（评审稿）

## 1. 目标与边界

基于现有 [`chat-bot.md`](C:/Users/yaozi/Desktop/code/raricy_bot/chat-bot.md) 契约，建设一个独立运行的站内通用助手：

- 以普通 core+ 账号登录，不改动网站或申请专用机器人接口。
- 大区仅响应对机器人用户名的精确 `@`；私聊响应所有有效文本。
- 使用 OpenAI 兼容的 `/v1/chat/completions` 模型服务。
- 默认跟随用户语言，回复简洁友好，不冒充真人或站方。
- 第一版不支持图片理解、博客理解、工具调用、联网搜索或长期用户记忆。

机器人资料必须明确标注“机器人”及“消息可能发送至第三方模型处理”。

## 2. 系统设计

### 2.1 技术与部署

- Python 3.12、asyncio 单进程服务。
- `httpx`/SSE 客户端接入网站，OpenAI Python SDK调用兼容模型。
- Linux Docker Compose 部署，固定单副本运行。
- 容器以非 root 用户运行：
  - 配置 YAML 只读挂载；
  - SQLite 数据目录单独持久化；
  - 密钥仅通过环境变量或 Docker secret 注入；
  - `restart: unless-stopped`。
- 内建运维端点：
  - `/livez`：事件循环及关键任务存活；
  - `/readyz`：已登录、SSE 已连接、工作队列可用。
- Docker 健康检查使用 `/livez`，避免站点临时故障造成重启风暴。

### 2.2 组件

1. **认证客户端**

   - 登录并保存内存中的 `raricy_session` Cookie。
   - 请求不设置 `Origin` 或 `Referer`。
   - `401` 时单飞重新登录，并重建 SSE。
2. **SSE 接收器**

   - 维护唯一 `/api/chat/stream` 连接。
   - 解析默认 SSE message 中的 JSON `type`。
   - 保存并在重连时发送 `Last-Event-ID`。
   - 断线以 3 秒为基础、最高 60 秒并带抖动重连。
3. **消息路由器**

   - 忽略机器人自身消息、删除消息、拍一拍、typing 和 read 事件。
   - 大区精确匹配区分大小写的 `@<机器人用户名>`，并要求用户名两侧不是用户名合法字符。
   - 私聊处理所有非空文本。
   - 将符合条件的请求送入容量为 50 的有界队列。
4. **上下文管理器**

   - 私聊以 `channel_id` 隔离。
   - 大区以 `lobby + author.id` 隔离，避免不同用户串话。
   - 每个会话最多保留最近 10 个用户—助手轮次，再按约 8k 输入 tokens 裁剪。
   - 上下文仅保存在内存，进程重启后清空。
   - 大区只纳入直接 @ 机器人的消息、机器人回复及当前 `reply_to` 文本，不外送旁观消息。
5. **模型工作器**

   - 同一会话严格串行，全局最多并发 3 个模型请求。
   - 默认超时 45 秒、温度 0.4、最多输出 600 tokens。
   - 网络错误、408、429 和 5xx 最多短重试一次；仍失败时使用本地故障提示。
   - 不可重试的 4xx 不重试。
6. **消息发送器**

   - 所有回复均带 `reply_to=<触发消息 id>`，包括私聊，以便关联和恢复。
   - 判断成功只检查响应 `code === 200`；正确处理成功响应中 `message` 为对象的例外。
   - 发送前经过本地限频器，并检查输出中是否意外包含已加载的密码或 API Key；命中时替换为 `[redacted]`。

### 2.3 消息行为

- 大区支持：
  - `@bot <问题>`
  - `@bot /help`
  - `@bot /reset`
- 私聊支持：
  - 普通文本；
  - `/help`；
  - `/reset`。
- `/help` 返回能力、隐私、无联网/图片能力说明，不调用模型。
- `/reset` 仅清除调用者当前会话的内存上下文，不删除站内消息。
- 文本附带图片或博客时只处理文本；纯图片或纯博客消息返回一次“不支持”提示。
- 超过 8,000 字符的单条输入不调用模型，提示用户缩短。
- 空白内容或仅包含 `@bot` 时返回本地用法提示。
- 模型输出超过 4,000 字符时在自然段边界截断并添加省略提示。
- 本地规则直接拒绝索取系统提示、运行密钥或隐藏配置的请求；其他高风险内容由系统提示约束。

## 3. 状态、可靠性与配额

### 3.1 SQLite 元数据

SQLite 只保存以下运行状态，不保存消息正文或模型输入输出：

- 已接收事件 id、接收顺序、频道 id及处理状态；
- 已知私聊频道及最后见到的消息 id；
- 已完成回复对应的站内消息 id；
- 发送尝试时间；
- 忙碌、故障和配额提示的冷却状态；
- 可安全提交的 `Last-Event-ID` 水位。

处理完成或明确忽略的事件才能推进 SSE 水位。崩溃时，未完成事件依靠旧水位重新补发。

### 3.2 重复回复控制

- `message.id` 是主要去重键。
- POST 成功后再将事件标记为完成。
- 如果发送结果不确定，先查询对应频道中 `after=<触发消息 id>` 的消息，寻找机器人发送且 `reply.id` 等于触发消息 id 的记录。
- 找到则视为已成功；未找到才允许一次重发。
- 上游没有幂等键，因此极端网络故障下无法保证严格 exactly-once；设计目标为可恢复的 best-effort 去重。

### 3.3 resync

收到 `resync` 时：

1. 拉取大区最新 100 条；
2. 拉取 SQLite 中所有已知私聊频道的最新 100 条；
3. 按全局消息 id 合并排序；
4. 交给相同的路由和去重流程；
5. 恢复实时流处理。

当前接口无法列出机器人尚未知晓的私聊频道。因此，积压超过 100 条期间首次出现的新私聊可能无法恢复；这是现有公开契约的已知限制。

### 3.4 配额保护

- 所有消息 POST 尝试共享每分钟 25 次的本地令牌桶，低于站点的 30 次硬限制。
- 滚动 24 小时内：
  - 前 750 次用于正常回复；
  - 达到 750 后停止模型回复；
  - 控制提示最多使用到总计 790 次，每频道每 24 小时最多一次；
  - 达到 790 后完全静默，预留 10 次应对计数偏差。
- 队列满时，每频道 5 分钟最多发送一次忙碌提示。
- 模型最终失败时，每频道 5 分钟最多发送一次故障提示。
- 站点 `429` 优先遵循 `Retry-After`；没有该头时等待 60 秒。

### 3.5 权限与会话故障

- `401`：重新登录一次并重连 SSE。
- `403 需要核心用户权限` 或禁言：进入不可就绪状态，每 5 分钟探测恢复，不持续刷日志或发送消息。
- CSRF 相关 `403`：视为客户端配置错误，确认没有伪造 `Origin`/`Referer`。
- 触发消息在回复前被删除导致 `reply_to` 无效时，放弃回复并将事件标记为跳过。

## 4. 配置与安全

非敏感配置写入只读 YAML：

```yaml
site:
  base_url: "https://example.com"

model:
  base_url: "https://provider.example/v1"
  model: "configured-model-name"
  temperature: 0.4
  timeout_seconds: 45
  max_output_tokens: 600

behavior:
  context_turns: 10
  context_input_tokens: 8000
  max_input_chars: 8000
  max_output_chars: 4000
  concurrency: 3
  queue_size: 50
  minute_attempt_limit: 25
  daily_normal_limit: 750
  daily_absolute_limit: 790
  notice_cooldown_seconds: 300

system_prompt: |
  你是站内通用聊天机器人。明确自己的机器人身份，
  跟随用户语言，保持简洁友好，不代表站方作正式承诺。
```

敏感配置仅使用：

- `RARICY_USERNAME`
- `RARICY_PASSWORD`
- `LLM_API_KEY`
- 可选 `BOT_CONFIG_PATH`

安全要求：

- 不将 Cookie、密码、API Key、正文或模型请求体写入日志。
- 极简日志仅记录启动/停止、组件名、错误类别、HTTP 状态和事件 id。
- SQLite 文件、备份和容器卷不得包含对话内容。
- HTTP 客户端关闭 debug/body logging。
- 用户内容和引用文本始终作为不可信数据放在 user 消息中，不拼接进 system prompt。
- 第一版没有执行代码、浏览网页、访问服务器文件或调用站内管理接口的能力。

## 5. 验证与验收

### 自动化测试

- 精确 @、近似用户名、大小写、用户名边界和纯 @ 测试。
- 大区按用户隔离、私聊按频道隔离、10 轮/8k tokens 裁剪测试。
- `/help`、`/reset`、图片/博客降级、超长输入和自身消息忽略测试。
- 登录 Cookie、401 重登、SSE 重连、`Last-Event-ID` 和 `resync` 集成测试。
- 发送成功的特殊信封、429、禁言、删除引用和模型错误测试。
- 并发 3、同会话顺序、队列 50、忙碌提示冷却测试。
- SQLite 崩溃恢复、补发去重和不确定发送结果对账测试。
- 750/790 配额边界和并发原子计数测试。
- 断言数据库与日志中不存在正文、Cookie、密码和 API Key。

### 预发布验收

- 人工创建并提升测试机器人为 core+。
- 验证大区普通消息不触发、精确 @ 触发、私聊触发。
- 验证回复均引用原消息且机器人自己的 SSE 回显不会形成循环。
- 主动断网后恢复，确认补发不会重复回复。
- 重启容器，确认对话记忆清空但去重和配额状态保留。
- 模拟模型超时、网站 401/403/429 和队列满载。
- 验证 `/livez`、`/readyz`、Compose 自动拉起及优雅关闭。
- 上线前检查机器人用户名或简介已披露机器人身份和第三方模型处理行为。

### 已锁定假设

- 网站接口以现有 `chat-bot.md` 为唯一可用契约，本项目不修改网站代码。
- 仅运行一个机器人实例；多副本选主和分布式队列不属于第一版。
- 短期上下文在重启后丢失是可接受行为。
- 不要求流式展示模型生成过程，模型完整生成后一次性 POST。
- 未知私聊在超大 SSE 缺口中的恢复限制，以及缺少发送幂等键的限制，作为首版明确接受的上游约束。
